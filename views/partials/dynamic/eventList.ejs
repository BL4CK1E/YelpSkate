<div class="container">
    <h2 class="text-center display-4">Upcoming Events</h2>
    <br>
    <div class="row mb-2">

        <%  for (let i = 0; i < db.events.length; i++) { %>

            <div class="evntr_card col-md-4">
                <a href="/view/<%= i %>">
                    <img class="evntr_img_overlay" src="<%= db.events[i].picture %>">
                </a>
                <div class="evntr_card_date text-center">
                    <span class="blockquote">
                        <%= db.events[i].eventDate %>
                    </span>
                </div>
                <div class="evntr_card_info text-center">
                    <h5 class="blockquote mb-0 p-1">
                        <%= db.events[i].name %>
                    </h5>
                </div>
            </div>

            <!-- <div class="asc_card card mb-4 box-shadow">
                    <img class="card-img-top" src="<%= db.events[i].picture %>" alt="Card image cap">
                    <div class="card-body d-flex flex-column justify-content-between">
                        <div>
                            <h3 class="m-0 p-0">
                                <a class="text-dark m-0 p-0" href="/home/<%=i%>">
                                    <%= db.events[i].city %>
                                </a>
                            </h3>
                            <div class="mb-1 text-muted small">
                                <%= db.events[i].name %>
                            </div>
                            <p class="shortOverview card-text mb-auto">
                                <%= db.events[i].shortOverview %>
                            </p>
                        </div>
                        <div>
                            <a href="/view/<%=i%>">
                                <button class="btn btn-md btn-block btn-outline-dark">View Event Details</button>
                            </a>
                        </div>
                    </div>
                </div> -->

            <% } %>

    </div>
</div>