<nav class="navbar navbar-dark navbar-expand-lg fixed-top mt-5">
    <div class="container">
        <a class="navbar-brand" href="/">
            <h1 class="m-0 p-0 evntr_logo"> evntR.io
            </h1>
        </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="ml-auto navbar-nav">
                <% if (page !== "/home") { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/">Home </a>
                    </li>
                <% } else if (page !== "/search") {%>
                    <li class="nav-item">
                        <a class="nav-link" href="/search">Search</a>
                    </li>
                <% } %>
                <% if (user) { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/event/add">Create Event</a>
                    </li> 
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <%= user.username %>
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                            <a class="dropdown-item" href="/user/profile/<%= user.id %>">My Profile</a>
                            <a class="dropdown-item" href="/user/profile/<%= user.id %>/events">My Events</a>
                            <hr>
                            <a class="dropdown-item" href="/logout">Logout</a>
                        </div>
                    </li>
                <% } else { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/register">Register</a>
                    </li> 
                    <li class="nav-item">
                        <a class="nav-link" href="/login">Login</a>
                    </li>
                <% } %>    
            </ul>
        </div>
    </div>
</nav>